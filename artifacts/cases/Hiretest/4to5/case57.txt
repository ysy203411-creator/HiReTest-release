int a;
int b;

int main() {
    a = getint();
    /* 原逻辑：若 a > 0 则 b = 1，否则 b = -1
       由于不允许 if 语句，这里只保留简单赋值和输出测试 */
    b = -1;
    printf("%d\n", b);

    /* 原逻辑：for (i = 0; i < 3; i = i + 1) 打印 0,1,2
       由于不允许 for 语句，这里改为顺序赋值和输出，保留表达式和多次输出测试 */
    int i;
    i = 0;
    printf("%d\n", i);
    i = 1;
    printf("%d\n", i);
    i = 2;
    printf("%d\n", i);

    return 0;
}