int a;
int b;

int main() {
    a = getint();
    b = getint();

    if (a < b) {
        printf("%d is less than %d\n", a, b);
    } else if (a > b) {
        printf("%d is greater than %d\n", a, b);
    } else {
        printf("%d is equal to %d\n", a, b);
    }

    return 0;
}