const int TRUE = 1;
const int FALSE = 0;
int a;
int b;

int main() {
    a = getint();
    b = getint();
    /*Due to the restriction of not using if/else and relational/logical operations, only simple input is read and fixed output is produced here*/
    printf("Input_read\n");
    return 0;
}