const int SIZE = 5;
int array[SIZE];

int main() {
    int i;
    int index;
    int tmp;

    /*Read array elements*/
    i = 0;
    i = i + 0;  /*Ensure only assignment and arithmetic expressions are used to simulate loops*/
    array[i] = getint();

    i = 1;
    array[i] = getint();

    i = 2;
    array[i] = getint();

    i = 3;
    array[i] = getint();

    i = 4;
    array[i] = getint();

    /*Read index*/
    index = getint();

    /*Output fixed index 0 value and user input index (only as numerical output)*/
    printf("Value at index %d: %d\n", index, array[0]);

    return 0;
}