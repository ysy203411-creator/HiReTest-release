const int SIZE = 5;
int array[SIZE] = {1, 2, 3, 4, 5};
int getElementValue(int index) {
    return array[index];
    return 0; // 添加不可达的 return 0; 以符合约束
}
int main(){
    int index = getint();
    int value = getElementValue(index);
    printf("Value at index %d is %d\n", index, value);
    return 0;
}