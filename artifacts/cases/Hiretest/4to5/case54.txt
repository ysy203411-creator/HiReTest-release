const int ARRAY_SIZE = 5;
const int INIT_VALUES[5] = {1, 2, 3, 0, 0}; //Convert to a one-dimensional array, and provide all elements

int main() {
    int i;
    int array[ARRAY_SIZE];

    for (i = 0; i < ARRAY_SIZE; i = i + 1) {
        array[i] = INIT_VALUES[i];
    }

    printf("Array values: ");
    for (i = 0; i < ARRAY_SIZE; i = i + 1) {
        printf("%d ", array[i]);
    }
    printf("\n");

    return 0;
}