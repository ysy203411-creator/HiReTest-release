int globalVar;
const int constantValue = 10;
int add(int a, int b) {
    int result = a + b;
    return result;
}
void printSum(int a, int b) {
    int sum = add(a, b);
    printf("Sum: %d\n", sum); //Ensure the format specifier %d matches the number of parameters
}
int main(){
    int x = getint();
    int y = getint();
    printSum(x, y);
    return 0;
}