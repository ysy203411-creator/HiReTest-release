int arr[5] = {1, 2, 3};  // 未显式初始化的元素默认补 0

void printArray() {
    int i = 0;
    for (; i < 5; i = i + 1) {
        printf("%d ", arr[i]);
    }
    printf("\n");
}

int main() {
    printArray();
    return 0;
}