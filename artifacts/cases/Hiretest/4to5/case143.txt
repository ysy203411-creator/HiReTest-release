int a;
const int b = 10;
int arr[5] = {1, 2, 3, 4, 5};
void process(int x) {
    if (x > 0) {
        printf("Positive: %d\n", x);
    } else {
        printf("Non-positive: %d\n", x);
    }
}
int main(){
    a = getint();
    process(a);
    int i = 0;
    while (i < 5) {
        printf("Array element %d: %d\n", i, arr[i]);
        i = i + 1;
    }
    return 0;
}