const int SIZE = 10;
int globalVar;
int array[SIZE];
void checkSymbolType(int digits) {
    if (digits == 32) {
        printf("%d\n", digits); //Change to use %d
    } 

    if (digits == 8) {
        printf("%d\n", digits); //Change to use %d
    } else {
        printf("%s\n", "Symbol Error!"); //Change to use %s /no_think
    }
}
int main(){
    int input;
    printf("%s", "Enter digits (8 or 32): "); //Change to use %s /no_think
    input = getint();
    checkSymbolType(input);
    return 0;
}