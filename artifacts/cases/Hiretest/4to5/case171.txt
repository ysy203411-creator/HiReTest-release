const int a = 10;
int b = 10; //Initialize global variable b with a constant expression

void initialize() {
    b = a + 5;
}

int main(){
    initialize();
    int result = b * 2;
    printf("%d\n", result); //Fix escape sequence to a valid format
    return 0;
}