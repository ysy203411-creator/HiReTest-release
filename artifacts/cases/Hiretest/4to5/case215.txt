const int SIZE = 5;
const int arr[SIZE] = {1, 2, 3, 4, 5};

int main() {
    int sum = 0;
    int i = 0;
    while (i < SIZE) {
        sum = sum + arr[i];
        i = i + 1;
    }
    printf("Sum of array elements: %d\n", sum);
    return 0;
}