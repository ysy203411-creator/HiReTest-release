int a;
int b;
int main(){
    int x;
    int y;
    x = getint();
    y = getint();
    a = (x > 0) ? 1 : ((x < 0) ? (y / x > 1) : 0); //Use the conditional operator instead of if-else
    b = (x < 0) ? 1 : ((x > 0) ? (y / x < 1) : 0); //Use the conditional operator instead of if-else
    printf("%d %d", a, b);
    return 0;
}