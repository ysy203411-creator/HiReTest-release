int globalVar;
const int CONST_VAL = 10;
int array[5] = {1, 2, 3, 0, 0};

void modifyArray(int arr[]) {
    int i = 0;
    /*Originally there was a while loop here, it is changed to modify only one element to avoid using loop statements*/
    arr[0] = arr[0] + CONST_VAL;
    return;
}

int main() {
    int localVar = getint();
    printf("Initial value: %d\n", localVar);

    modifyArray(array);

    int i = 0;
    /*Originally there was a while loop here, it is changed to output only one element to avoid using loop statements*/
    printf("Array[%d]: %d\n", i, array[i]);

    /*Originally there was an if-else statement here, it is changed to unconditionally output to avoid using conditional statements*/
    printf("Input processed\n");

    return 0;
}