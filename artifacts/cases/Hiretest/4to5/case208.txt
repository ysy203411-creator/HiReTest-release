int globalVar;
const int MAX = 10;
void printNumbers(int n) {
    int i = 0;
    while (i < n) {
        printf("%d\n", i);
        i = i + 1;
    }
}
int main(){
    int input;
    input = getint();
    if (input > MAX) {
        input = MAX;
    }
    printNumbers(input);
    return 0;
}