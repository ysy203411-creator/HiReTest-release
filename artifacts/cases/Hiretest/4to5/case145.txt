const int CONST_A = 5;
const int CONST_B = 10;
int array[3] = {1, 2, 3};

int sum(int a, int b) {
    return a + b;
}

void print_sum(int a, int b) {
    int result = sum(a, b);
    printf("Sum: %d\n", result);
}

int main() {
    int x = getint();
    int y = getint();
    print_sum(x, y);
    return 0;
}