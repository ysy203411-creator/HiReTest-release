const int SIZE = 5;
const int str[SIZE] = {72, 101, 108, 108, 111}; // ASCII values for "Hello"

void printArray() {
    int i = 0;
    while (i < SIZE) {
        printf("%d ", str[i]);
        i = i + 1;
    }
    printf("\n");
}

int main() {
    printArray();
    return 0;
}