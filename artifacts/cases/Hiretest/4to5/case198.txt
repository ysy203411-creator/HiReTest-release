const int a = 1;
const int b = 0;
int result;
void testLAndExp(int x, int y) {
    if (x * y) { //Use multiplication instead of logical AND
        printf("Both are true\n"); //Modify to not use format specifiers
    } else {
        printf("At least one is false\n"); //Modify to not use format specifiers
    }
}
int main(){
    int input1 = getint();
    int input2 = getint();
    result = a * b; //Use multiplication instead of logical AND
    printf("Result of a * b: %d\n", result);
    testLAndExp(input1, input2);
    return 0;
}