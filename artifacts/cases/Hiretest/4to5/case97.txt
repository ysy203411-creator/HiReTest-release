int a;
const int b = 10;
int sum(int x, int y) {
    return x + y;
}
void printSum(int x, int y) {
    int result = sum(x, y);
    printf("Sum of %d and %d is %d\n", x, y, result);
}
int main(){
    int x, y;
    x = getint();
    y = getint();
    printSum(x, y);
    return 0;
}