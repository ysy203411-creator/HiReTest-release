const int SIZE = 5;
int arr[5] = {1, 2, 3, 4, 5};

int main() {
    int sum = 0;
    int i = 0;
    sum = sum + arr[i];
    i = i + 1;
    sum = sum + arr[i];
    i = i + 1;
    sum = sum + arr[i];
    i = i + 1;
    sum = sum + arr[i];
    i = i + 1;
    sum = sum + arr[i];
    printf("%d\n", sum);
    return 0;
}