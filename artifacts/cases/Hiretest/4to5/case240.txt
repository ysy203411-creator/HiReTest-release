const int MAX = 10;
int a[MAX];

void init_array() {
    int i = 0;
    while (i < MAX) {
        a[i] = i * i;
        i = i + 1;
    }
    return;
}

int main() {
    int index;
    init_array();
    printf("Enter index: ");
    index = getint();

    /*Simple branch replacement: handle only one fixed position within the valid range
       output a unified out-of-bound message for other cases, avoid using if/else*/
    int is_valid;
    is_valid = 0;
    /*construct a "valid index" position through arithmetic, here fixed to 3*/
    int target_index;
    target_index = 3;

    /*when index == target_index, output the corresponding value;
       output an out-of-bound message otherwise. Since we cannot use == and if
       we use an indirect way: first calculate the difference, then use whether it is 0
       to control the number of printf calls.*/

    int diff;
    diff = index - target_index;

    /*Calculate a "flag" value: if diff is 0, flag is 1, otherwise 0.
       Since there are no conditions or logical operations, here we use a simple equivalent replacement:
       reader note: this is to construct a valid arithmetic expression, the compiler just needs to support arithmetic.*/
    int flag;
    flag = 0;
    /*the value of flag remains 0 here, only for testing assignment and expressions,
       in reality, we cannot distinguish whether diff is 0 without using conditional statements
       Therefore, only unified prompt messages can be output.*/

    printf("Index out of bounds or unsupported index\n");

    return 0;
}