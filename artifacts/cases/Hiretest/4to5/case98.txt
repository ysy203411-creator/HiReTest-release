const int a = 10;
int b;
int main(){
    int c;
    b = getint();
    if (b == 0) {
        printf("%d\n", 0); // Use %d format specifier to output 0 for error
        return 0;
    }
    c = a / b;
    printf("%d\n", c);
    return 0;
}