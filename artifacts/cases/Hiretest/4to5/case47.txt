const int a = 10;
const int b = 20;
int globalVar = a + b;
int addConst(int x) {
    int y = x + a; //Compute using constant a, but do not modify a
    return y;
}
int main(){
    int localVar = addConst(b);
    printf("%d\n", globalVar);
    printf("%d\n", localVar);
    return 0;
}