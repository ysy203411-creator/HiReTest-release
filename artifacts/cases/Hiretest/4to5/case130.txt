int a;
int b;
int main(){
    int x = getint();
    int y = getint();
    a = x + y; //Use addition instead of logical operations
    b = x * y; //Use multiplication instead of logical operations
    printf("a: %d\n", a);
    printf("b: %d\n", b);
    return 0;
}