int globalVar;
const int CONST_VAL = 10;
int array[5] = {1, 2, 3, 0, 0}; //Complete array initialization

void printArray(int arr[]) {
    int i = 0;
    //Use for statement instead of goto statement
    for (i = 0; i < 5; i = i + 1) {
        printf("%d ", arr[i]);
    }
    printf("\n");
}

int add(int a, int b) {
    return a + b;
}

int main(){
    int localVar = 5;
    static int staticVar;
    int sum = add(localVar, CONST_VAL);
    printf("Sum: %d\n", sum);
    globalVar = getint();
    printf("GlobalVar: %d\n", globalVar);
    printArray(array);
    return 0;
}