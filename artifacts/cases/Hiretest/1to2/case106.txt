const int MAX = 10;
int numbers[MAX];
int add(int a, int b) {
    return a + b;
}
int main(){
    int result = add(5, 3);
    int error = 5; //Remove illegal symbol |, keep syntax and semantics correct
    printf("Result: %d\n", result);
    printf("Error: %d\n", error);
    return 0;
}