const int MAX = 100;
int global_array[MAX];
int add(int a, int b) {
    return a + b;
}
void print_result(int result) {
    printf("Result: %d\n", result);
}
int main(){
    int a = 10;
    int b = 20;
    int result = add(a, b);
    print_result(result);
    
    if (a || b) { // Corrected the illegal use of '|'
        printf("This should not print.\n");
    }
    return 0;
}