const int MAX = 5;
int x = 10;
int arr[MAX] = {1, 2, 3, 4, 5};
void printArray(int arr[], int size) {
    int i = 0;
    for (i = 0; i < size; i = i + 1) {
        printf("%d\n", arr[i]);
    }
    return;
}
int main(){
    int a = 5;
    int b = 10;
    int result = 0;
    if (a > b | a < b) { // 错误：使用单独的 |
        result = a + b;
    } else {
        result = a - b;
    }
    printf("Result: %d\n", result);
    printArray(arr, MAX);
    return 0;
}