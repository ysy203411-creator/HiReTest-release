const int a = 5;
int b;
int c[10];
void foo(int x) {
    int y = x + a;
    printf("%d\n", y);
}
int main(){
    int z = 0;
    int arr[5];
    arr[0] = 1;
    arr[1] = 2;
    arr[2] = 3;
    arr[3] = 4;
    arr[4] = 5;
    // 修改 printf 参数，去掉非法符号 |，改为合法的算术操作
    printf("%d\n", arr[0] + arr[1]);
    return 0;
}