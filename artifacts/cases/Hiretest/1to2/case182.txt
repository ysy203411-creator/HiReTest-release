const int a = 5;
const int b = 10;
int arr[3] = {1, 2, 3};
int sum(int x, int y) {
    return x + y;
}
int main(){
    const int c = a + b; //Use a legal constant expression to initialize a constant
    int result = sum(arr[0], arr[1]);
    printf("Result: %d\n", result);
    return 0;
}