const int a = 5;
const int b = 10;
int arr[3] = {1, 2, 3};
int sum(int x, int y) {
    return x + y;
}
int main(){
    const int c = a + b; // 使用合法的常量表达式初始化常量
    int result = sum(arr[0], arr[1]);
    printf("Result: %d\n", result);
    return 0;
}