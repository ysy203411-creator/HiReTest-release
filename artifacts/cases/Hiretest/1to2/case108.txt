const int a = 5;
int b = 10;
int add(int x, int y) {
    return x + y;
}
int main(){
    int result = add(a, b);
    if (result & 1) { //Using & alone as a logical operator should be an error
        printf("Result is odd\n");
    } else {
        printf("Result is even\n");
    }
    return 0;
}