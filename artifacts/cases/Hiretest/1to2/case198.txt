const int TRUE = 1;
const int FALSE = 0;
int globalVar = 0;
int checkCondition(int a, int b) {
    return a && b;
}
int main(){
    int a = 1;
    int b = 0;
    int result = checkCondition(a, b);
    printf("Result: %d\n", result); // Expected output: Result: 0
    if (a & b) { // This line contains an error: single '&' should be '&&'
        printf("This should not print.\n");
    }
    if (a && b) {
        printf("This should not print either.\n");
    } else {
        printf("Correctly identified false condition.\n"); // Expected output
    }
    return 0;
}