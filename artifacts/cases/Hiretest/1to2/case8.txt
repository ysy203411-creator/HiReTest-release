const int MAX = 100;
int add(int a, int b) {
    return a + b;
}
void printSum(int a, int b) {
    printf("Sum: %d\n", add(a, b));
    return;
}
int main(){
    int x = 10;
    int y = 20;
    if (x < y) {
        printf("x is less than y\n");
    }
    printSum(x, y);
    return 0; //Modify: Remove illegal symbol |, keep within constraints
    return 0;
}