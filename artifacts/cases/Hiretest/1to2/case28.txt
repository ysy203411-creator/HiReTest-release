const int SIZE = 10;
int numbers[SIZE];
void initializeArray(int arr[]) {
    int i = 0;
    for (i = 0; i < SIZE; i = i + 1) {
        arr[i] = i * i;
    }
}
int main(){
    int data[SIZE];
    initializeArray(data);
    int i = 0;
    for (i = 0; i < SIZE; i = i + 1) {
        printf("Element %d: %d\n", i, data[i]);
    }
    return 0;
}