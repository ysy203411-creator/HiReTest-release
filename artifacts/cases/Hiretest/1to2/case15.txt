const int MAX = 100;
int globalArray[MAX];
int add(int a, int b) {
    return a + b;
}
void printResult(int result) {
    printf("Result: %d\n", result);
}
int main(){
    int x = 10;
    int y = 20;
    int result = add(x, y);
    printResult(result);
    if (x > 5 & y < 25) { //Error: Using separate & symbols
        printf("This line should not compile.\n");
    }
    return 0;
}