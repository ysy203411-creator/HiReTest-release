const int MAX = 10;
int arr[MAX];
int sum(int a, int b) {
    return a + b;
}
int main(){
    int x = 5;
    int y = 3;
    int result = sum(x, y);
    if (result & x) { //Error: single & symbol used
        printf("Result is: %d\n", result);
    }
    return 0;
}