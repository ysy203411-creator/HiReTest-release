const int MAX = 10;
int arr[MAX];
int sum(int a, int b) {
    return a + b;
}
void printResult(int result) {
    printf("Result: %d\n", result);
}
int main(){
    int x = getint();
    int y = getint();
    int result = sum(x, y);
    printResult(result);
    if (x > 0 & y > 0) {  //Error: using separate &
        printf("Both numbers are positive.\n");
    }
    return 0;
}