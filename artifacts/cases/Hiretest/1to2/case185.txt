const int N = 10;
int array[N];
int main(){
    const int M = 5;
    int values[M] = {1, 2, 3, 4, 5};
    int x = 0;
    if (x & 1) { //Error: Using a standalone & is considered an illegal symbol
        printf("Odd\n");
    } else {
        printf("Even\n");
    }
    return 0;
}