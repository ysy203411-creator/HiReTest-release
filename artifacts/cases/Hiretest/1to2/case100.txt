const int MAX = 100;
int array[MAX];
int add(int a, int b) {
    return a + b;
}
void printResult(int result) {
    printf("Result: %d\n", result);
}
int main(){
    int x = 10;
    int y = 20;
    int result = add(x, y);
    printResult(result);
    int z = 0; // 修改为合法的赋值语句
    if (x != 0 && y != 0) {
        z = 1;
    }
    return 0;
}