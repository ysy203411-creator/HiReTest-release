const int CONST_VAL = 10;
int globalVar = 5;
int add(int a, int b) {
    return a + b;
}
void printResult(int result) {
    printf("Result is: %d\n", result);
}
int main(){
    int localVar = 3;
    int result = add(globalVar, localVar);
    printResult(result);
    if (result > 10 & result < 20) { // Use of a separate & should be considered an illegal symbol
        printf("This line contains an error.\n");
    }
    return 0;
}