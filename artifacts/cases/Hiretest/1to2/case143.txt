const int MAX = 100;
int globalVar = 10;
int add(int a, int b) {
    return a + b;
}
void printSum(int x, int y) {
    int sum = add(x, y);
    printf("Sum is: %d\n", sum);
}
int main(){
    int a = 5;
    int b = 15;
    int result = add(a, b);
    printf("Result: %d\n", result);
    printSum(globalVar, 20);
    if (a & b) { // Intentional error: single '&' instead of '&&'
        printf("This line has an error\n");
    }
    return 0;
}