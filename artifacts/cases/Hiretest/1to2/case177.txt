const int MAX = 100;
int globalVar = 10;
int add(int a, int b) {
    return a + b;
}
void printSum(int x, int y) {
    int sum = add(x, y);
    printf("Sum: %d\n", sum);
}
int main(){
    int a = 5;
    int b = 15;
    int result = add(a, b);
    printf("Result: %d\n", result);
    if (result > 10) {
        printSum(a, b);
    } else {
        printf("Result is less than or equal to 10\n");
    }
    return 0;
}