const int MAX_LENGTH = 100;
int globalArray[MAX_LENGTH];
void writeToFile(int content, int fileIndex) {
    //Simulate file write operation
    printf("Writing to file index: %d, Content: %d\n", fileIndex, content);
}
void testWriteToFile() {
    int content = 42; //Use integers instead of strings
    int fileIndex = 24; //Use integers instead of strings
    writeToFile(content, fileIndex);
}
int main(){
    int localArray[10] = {0}; //Initialize array
    int i;
    for (i = 0; i < 10; i = i + 1) {
        localArray[i] = i * i;
    }
    testWriteToFile();
    for (i = 0; i < 10; i = i + 1) {
        printf("localArray[%d] = %d\n", i, localArray[i]);
    }
    return 0;
}