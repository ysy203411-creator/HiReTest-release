const int MAX_LENGTH = 1024;
int globalArray[MAX_LENGTH] = {0};
void checkStorageLength() {
    int length = getint(); //Assume input value greater than storage length
    if (length > MAX_LENGTH) {
        printf("Length exceeds storage capacity: %d\n", length);
    } else {
        printf("Length is within storage capacity: %d\n", length);
    }
}
int main(){
    int input;
    printf("Enter a number: ");
    input = getint();
    
    if (input & 1) { //Using separate &, should be considered illegal symbol
        printf("Odd number\n");
    } else {
        printf("Even number\n");
    }
    
    checkStorageLength();
    
    return 0;
}