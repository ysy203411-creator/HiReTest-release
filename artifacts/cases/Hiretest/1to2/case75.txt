const int MAX = 10;
int arr[MAX];
int add(int a, int b) {
    return a + b;
}
int main(){
    int x = 5;
    int y = 10;
    int result = add(x, y);
    printf("Result: %d\n", result);
    //Remove illegal symbol |, keep test case design purpose
    int invalidResult = add(x, y);
    printf("Invalid Result: %d\n", invalidResult);
    return 0;
}