int global_var = 10;
const int CONST_VAR = 20;
int add(int a, int b) {
    return a + b;
}
int subtract(int a, int b) {
    return a - b;
}
int main(){
    int x = 5;
    int y = 10;
    int result;
    //Use the & symbol separately to check if errors can be correctly identified
    if (x & y) {
        result = add(x, y);
    } else {
        result = subtract(x, y);
    }
    printf("Result: %d\n", result);
    return 0;
}