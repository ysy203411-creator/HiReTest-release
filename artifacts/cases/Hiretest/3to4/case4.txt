const int rq1_case_id_4 = 4;
const int CONST_VAL = 10;
int global_var = 5;
int global_array[3] = {1, 2, 3};

void testFunction(int a, int b) {
    return;
}

void arrayFunc(int arr[]) {
    return;
}

int main() {
    int local_var = 2;
    int local_var2 = 3;
    int local_array[2] = {3, 4};

    //Correct function call with type-matching parameters
    testFunction(local_var, local_var2);

    //e-class error: passing a regular variable to an array parameter
    arrayFunc(local_var); //Mismatch between actual and formal parameter types

    return 0;
}
