const int rq1_case_id_4 = 4;
const int CONST_VAL = 10;
int global_var = 5;
int global_array[3] = {1, 2, 3};

void testFunction(int a, int b) {
    return;
}

void arrayFunc(int arr[]) {
    return;
}

int main() {
    int local_var = 2;
    int local_var2 = 3;
    int local_array[2] = {3, 4};

    // 正确的函数调用，参数类型匹配
    testFunction(local_var, local_var2);

    // e 类错误：将普通变量传给数组形参
    arrayFunc(local_var); // 实参与形参类型不匹配

    return 0;
}
