const int rq1_case_id_64 = 64;
const int MAX = 10;
int globalArray[MAX];
int globalVar;

void updateScope(int arr[], int size) {
    int i = 0;
    for (i = 0; i < size; i = i + 1) {
        arr[i] = i * 2;
    }
}

int main() {
    int localVar = 5;
    const int localConst = 3;
    int localArray[3]; // 修改为使用字面量常量
    updateScope(globalArray, MAX);
    int i = 0;
    for (i = 0; i < MAX; i = i + 1) {
        printf("globalArray[%d] = %d\n", i, globalArray[i]);
    }
    return 0;
}
