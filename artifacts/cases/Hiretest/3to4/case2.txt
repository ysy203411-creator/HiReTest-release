const int rq1_case_id_2 = 2;
const int SIZE = 5;
int globalArray[SIZE];
void modifyArray(int index, int value) {
    if (index >= 0 && index < SIZE) {
        globalArray[index] = value; //Correct use of lvalue
    }
}
int main(){
    int localArray[SIZE];
    int i = 0;
    for (i = 0; i < SIZE; i = i + 1) {
        localArray[i] = i * 2; //Correct use of lvalue
    }
    modifyArray(2, 10);
    printf("globalArray[2] = %d\n", globalArray[2]); //Check modification
    printf("localArray[2] = %d\n", localArray[2]);   //Check initialization
    return 0;
}
