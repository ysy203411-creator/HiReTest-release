const int rq1_case_id_54 = 54;
const int MAX = 100;
int x;
int y;
void check(int a) {
    if (a > 0) {
        printf("Positive\n");
    } else {
        printf("Non-positive\n");
    }
    return;
}
int main(){
    x = getint();
    y = getint();
    if (x > y) {
        printf("x is greater than y\n");
    } else {
        printf("x is not greater than y\n");
    }
    int i = 0;
    for (i = 0; i < MAX; i = i + 1) {
        x = x + i;
        if (x > 50) {
            break;
        }
    }
    check(x);
    return 0;
}
