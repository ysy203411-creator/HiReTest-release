const int rq1_case_id_1 = 1;
const int a = 5;
int b = 10;
int arr[5] = {1, 2, 3, 4, 5};
void func() {
    printf("Inside func\n");
    return; // Correct usage of return in void function
}
int add(int x, int y) {
    return x + y; // Correct usage of return in int function
}
int main(){
    int x = getint();
    int result = add(x, b); // Correct function call with matching parameters
    printf("Result: %d\n", result); // Correct printf usage with matching %d
    func(); // Correct function call with no parameters
    return 0; // Correct return statement in main function
}
