const int rq1_case_id_57 = 57;
const int CONST_VAL = 5;
int global_var = 10;
int array[5] = {1, 2, 3, 4, 5};
void modifyArray(int arr[]) {
    arr[0] = 100; //Modify the first element of the array
}
int main(){
    int local_var = CONST_VAL;
    int another_array[3] = {7, 8, 9};
    modifyArray(array); //Correct call, passing an array
    printf("Array[0]: %d\n", array[0]); //Should output 100
    //Incorrect call, passing a variable instead of an array
    modifyArray(local_var); //Should detect an error: actual and formal parameter types do not match
    return 0;
}
