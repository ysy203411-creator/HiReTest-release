const int rq1_case_id_23 = 23;
const int MAX = 100;
int arr[MAX];
void process(int x, int[] data) {
    printf("Processing %d\n", x);
}
void processByte(int x) {
    printf("Byte processing %d\n", x);
}
int main(){
    int value = 42;
    int byteValue = 127;
    process(value, arr); //Normal call, type matches
    processByte(byteValue); //Normal call, type matches
    return 0;
}
