const int rq1_case_id_69 = 69;
const int SIZE = 5;
int arr[SIZE] = {1, 2, 3, 4, 5};
const int CONST_VAL = 10;
int compare(int a, int b) {
    //Modify return value to arithmetic expression, comply with constraints
    return a - b;
}
void print_result(int result) {
    //Replace relational operators with arithmetic expression to maintain original meaning
    if (result - 0) {
        printf("Comparison is true\n");
    } else {
        printf("Comparison is false\n");
    }
}
int main(){
    int x = getint();
    int y = arr[2];
    int result = compare(x, y);
    print_result(result);
    return 0;
}
