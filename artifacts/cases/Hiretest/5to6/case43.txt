const int SIZE = 5;
int globalArray[SIZE] = {1, 2, 3, 0, 0}; // 初始化列表填充0
void printArray(int arr[], int size) {
    int i = 0;
    for (i = 0; i < size; i = i + 1) {
        printf("%d ", arr[i]);
    }
    printf("\n");
}
int main(){
    printArray(globalArray, SIZE);
    return 0;
}