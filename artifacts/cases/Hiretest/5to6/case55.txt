const int MAX_CHAR = 255;

int main() {
    int input;
    printf("Enter a character 0-255: ");
    input = getint();
    if (input < 0 || input > MAX_CHAR) {
        printf("Invalid input\n");
    } else {
        printf("Character entered: %d\n", input);
    }
    return 0;
}