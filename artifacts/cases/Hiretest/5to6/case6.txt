const int a = 5;
int b = 10;
int arr[3] = {1, 2, 3};
void printArray(int arr[]) {
    int i = 0;
    for (i = 0; i < 3; i = i + 1) {
        printf("%d ", arr[i]);
    }
    printf("\n");
}
int main(){
    int x;
    x = getint();
    if (x > a) { // 直接在if条件中使用关系运算符
        printf("x is greater than a\n");
    } else {
        printf("x is not greater than a\n");
    }
    printArray(arr);
    return 0;
}