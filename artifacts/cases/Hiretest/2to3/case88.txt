const int a = 5;
int b = 0; //Fix: global variable b is initialized to 0
int add(int x, int y) {
    return x + y;
}
int main(){
    int c;
    c = add(a, b); //b is already initialized, call is valid
    printf("%d\n", c);
    return 0;
}