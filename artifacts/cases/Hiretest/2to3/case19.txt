const int MAX = 100;
int numbers[MAX];
int add(int a, int b) {
    return a + b;
}
void printSum(int arr[], int size) {  //Modify function definition to comply with constraints
    int sum = 0;
    int i = 0;
    for (i = 0; i < size; i = i + 1) {
        sum = add(sum, arr[i]);
    }
    printf("Sum: %d\n", sum);
}
int main(){
    int i = 0;
    for (i = 0; i < MAX; i = i + 1) {
        numbers[i] = i;
    }
    printSum(numbers, MAX);  //Pass array name when calling
    return 0;
}