const int MAX = 10;
int arr[MAX];
int add(int x, int y) {
    return x + y;
}
void printArray(int arr[], int size) {
    int i = 0;
    for (i = 0; i < size; i = i + 1) {
        printf("%d ", arr[i]);
    }
    printf("\n");
}
int main(){
    int a = 5;
    int b = 3;
    int c;
    c = add(a, b);
    printf("Sum: %d\n", c);
    int i = 0;
    for (i = 0; i < MAX; i = i + 1) {
        arr[i] = i * 2;
    }
    printArray(arr, MAX);
    if (c > 5) {
        printf("c is greater than 5\n");
    } else {
        printf("c is not greater than 5\n");
    }
    return 0;
}