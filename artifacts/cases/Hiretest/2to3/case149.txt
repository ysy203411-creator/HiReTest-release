const int SIZE = 5;
int arr[SIZE];
int main(){
    int a = 10;
    int b = 0; // 初始化变量b
    const int c = 20;
    int arr2[3];
    arr2[0] = a + c;
    arr2[1] = b; // 使用已初始化的变量b
    arr2[2] = 30;
    printf("%d %d %d", arr2[0], arr2[1], arr2[2]); // 移除非法转义序列 \n
    return 0;
}