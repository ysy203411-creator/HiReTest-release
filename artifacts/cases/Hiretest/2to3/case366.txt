const int SIZE = 10;
int globalArray[SIZE];
int testFunc(int param) {
    int localVar = param * 2;
    if (localVar > 10) {
        return localVar;
    } else {
        return 0; //Change to return an integer literal
    }
    return 0; //Add an unreachable return statement to satisfy constraints
}
int main(){
    globalArray[0] = testFunc(5);
    printf("Result: %d\n", globalArray[0]);
    return 0;
}