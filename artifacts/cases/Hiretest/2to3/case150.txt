const int a = 5;
int b;
int arr[3];
void func() {
    b = a; //Normal assignment
    // a = 10; // Error: attempting to assign to a constant, remove this line to comply with constraints
}
int main(){
    arr[0] = 1;
    arr[1 = 2; // Missing closing square bracket, error type "k"
    b = getint(; // Missing closing parenthesis, error type "j"
    printf("%d\n", b);
    return 0;
}