const int MAX_SIZE = 10;
int arr[MAX_SIZE];
int func(int arr[) { //Missing closing bracket
    return arr[0];
}
int main(){
    int result = func(arr);
    printf("Result: %d\n", result);
    return 0;
}