const int SIZE = 5;
int array[SIZE] = {1, 2, 3, 4, 5};
void printArray(int arr2[], int len) {  //Rename parameter to avoid duplication
    int i = 0;
    for (i = 0; i < len; i = i + 1) {  //Rename used parameter to maintain consistency
        printf("%d ", arr2[i]);
    }
    printf("\n");
}
int main(){
    const int INIT_VAL = 10;
    int numbers[3] = {INIT_VAL, INIT_VAL + 1, INIT_VAL + 2};
    printArray(numbers, 3);
    return 0;
}