const int SIZE = 5;
int array[SIZE] = {1, 2, 3, 4, 5};
void printArray(int arr2[], int len) {  // 修改参数名以避免重名
    int i = 0;
    for (i = 0; i < len; i = i + 1) {  // 修改使用的参数名以保持一致
        printf("%d ", arr2[i]);
    }
    printf("\n");
}
int main(){
    const int INIT_VAL = 10;
    int numbers[3] = {INIT_VAL, INIT_VAL + 1, INIT_VAL + 2};
    printArray(numbers, 3);
    return 0;
}