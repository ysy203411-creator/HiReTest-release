const int SIZE = 5;
int errors[SIZE];
int globalIndex = 0; //Rename global variable
void addError(int errorCode) {
    if (globalIndex < SIZE) {
        errors[globalIndex] = errorCode;
        globalIndex = globalIndex + 1;
    }
}
void printErrors() {
    int i = 0;
    for (i = 0; i < SIZE; i = i + 1) {
        printf("%d\n", errors[i]);
    }
}
int main(){
    if (globalIndex < SIZE) { //Place condition in if statement
        addError(404);
    }
    if (globalIndex < SIZE) {
        addError(404); // Duplicate error code
    }
    if (globalIndex < SIZE) {
        addError(500);
    }
    if (globalIndex < SIZE) {
        addError(500); // Duplicate error code
    }
    if (globalIndex < SIZE) {
        addError(403);
    }
    printErrors(); // Should print errors without duplicates
    return 0;
}