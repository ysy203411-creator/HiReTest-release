const int MAX = 10;
int arr[MAX];
int sum(int a[], int size) {
    int total = 0;
    int i = 0;
    for (i = 0; i < size; i = i + 1) {
        total = total + a[i];
    }
    return total;
}
int main(){
    int i = 0;
    for (i = 0; i < MAX; i = i + 1) {
        arr[i] = i * i;
    }
    int result = sum(arr, MAX);
    printf("Sum of squares: %d\n", result);
    int missingSemicolon = 5 // i类错误：缺少分号
    int missingParenthesis = (5 * 3 // j类错误：缺少右小括号
    int missingBracket = arr[5 // k类错误：缺少右中括号
    return 0;
}