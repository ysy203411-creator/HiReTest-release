const int MAX = 10;
int globalVar = 5;
int add(int a, int b) {
    return a + b;
}
void printResult(int result) {
    printf("Result is: %d\n", result);
}
int main(){
    int x = 3;
    int y = 7;
    int result = add(x, y);
    printResult(result);
    if (x < y) {
        printf("x is less than y\n");
    } else {
        printf("x is not less than y\n");
    }
    int i = 0;
    for (i = 0; i < MAX; i = i + 1) {
        if (i == 5) {
            break;
        }
        printf("i: %d\n", i);
    }
    return 0;
}