const int size = 5;
int numbers[size] = {1, 2, 3, 4, 5};
int sum(int arr[], int len) {
    int total = 0;
    int i = 0;
    for (i = 0; i < len; i = i + 1) {
        total = total + arr[i];
    }
    return total;
}
int main(){
    int result;
    result = sum(numbers, size;
    printf("Sum: %d\n", result);
    return 0;
}