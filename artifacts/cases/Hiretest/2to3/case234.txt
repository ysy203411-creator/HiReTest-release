const int MAX = 100;
int add(int a, int b) {
    int result = a + b;
    return result; // 添加return语句
    return 0; // 符合约束7，添加不可达的return语句
}
int main(){
    int x = 10;
    int y = 20;
    int sum = add(x, y);
    printf("Sum: %d\n", sum);
    return 0;
}