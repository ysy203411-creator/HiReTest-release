const int a = 5;
int b = 10;
int c[3] = {1, 2, 3};
void printValues(int x, int y) {
    printf("Value of x: %d\n", x);
    printf("Value of y: %d\n", y);
}
int main(){
    int x = getint();
    int y = getint();
    printValues(x, y);
    int sum = a + b; /* This is a comment block
                      that spans multiple lines
                      and should be handled correctly */
    int product = a * b; // Single line comment
    printf("Sum: %d\n", sum);
    printf("Product: %d\n", product);
    return 0;
}