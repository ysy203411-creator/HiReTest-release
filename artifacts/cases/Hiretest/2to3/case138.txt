const int MAX_SIZE = 10;
int globalArray[MAX_SIZE];
void funcWithParams(int a, int b[]) {
    int localVar = a + b[0];
    printf("Local variable: %d\n", localVar);
    return;
}
int main(){
    int localArray[MAX_SIZE];
    int i = 0;
    for (i = 0; i < MAX_SIZE; i = i + 1) {
        localArray[i] = i;
    }
    //Keep j class error: missing right parenthesis
    funcWithParams(globalArray[0], globalArray; //Missing right parenthesis
    return 0;
}