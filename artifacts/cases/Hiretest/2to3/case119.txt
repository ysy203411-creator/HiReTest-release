const int a = 10;
int b = 5;
int c[5] = {1, 2, 3, 4, 5};
int add(int x, int y) {
    return x + y;
}
void printResult(int result) {
    printf("Result: %d\n", result);
}
int main(){
    int x = getint();
    int y = getint();
    int result = add(x, y);
    printResult(result);
    if (result > 10) {
        // 这里可以进行其他操作
    } else {
        // 这里可以进行其他操作
    }
    return 0;
}