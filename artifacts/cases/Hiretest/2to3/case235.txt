const int MAX = 10;
int arr[MAX];
int sum(int x, int y) {
    return x + y;
}
void printSum(int a, int b) {
    int result = sum(a, b);
    printf("Sum: %d\n", result); // 修改转义序列
}
int main(){
    int a = 5;
    int b = 3;
    arr[0] = a;
    arr[1] = b;
    printSum(arr[0], arr[1]);
    return 0; // 添加显式的返回值
}