int globalVar = 5;
const int CONST_VAR = 10;
int add(int a, int b) {
    return a + b;
}
void printResult(int result) {
    printf("Result: %d\n", result);
}
int main(){
    int a = 3;
    int b = 4;
    int result = add(a, b);
    printResult(result);
    if (a && b) { // Corrected to logical AND
        printf("Logical AND is true\n");
    }
    if (a || b) { // Corrected to logical OR
        printf("Logical OR is true\n");
    }
    return 0;
}