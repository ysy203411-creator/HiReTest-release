const int MAX = 100;
int foo(int a) {
    return a + 1;
}
int bar(int b) { //Rename parameters to avoid redefinition
    return b * 2;
}
int main(){
    int x = getint();
    int y = foo(x);
    printf("%d\n", y);
    return 0;
}