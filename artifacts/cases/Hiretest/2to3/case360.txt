const int MAX = 100;
int globalVar = 10;
int functionWithNoReturn() {
    return globalVar; //Keep the return value here
    return 0; //Add an explicit return statement
}
void printMessage() {
    printf("Hello, World!\n");
}
int main(){
    int localVar = 5;
    if (localVar > 0) {
        printMessage(); //Replace the return statement with a function call
    }
    printMessage();
    return 0;
}