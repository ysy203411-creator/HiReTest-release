const int MAX = 10;
int array[MAX];
int add(int a, int b) {
    return a + b;
}
void printError() {
    if (array[0] | array[1]) { //Use of illegal symbol |
        printf("Error: %d\n", array[0]);
    }
}
int main(){
    int x = 5;
    int y = 10;
    int result = add(x, y;  //Missing right parenthesis ), j class error
    printError();
    return 0;
}