const int MAX = 5;
int arr[MAX];
void func(int a, int b) {
    printf("%d %d\n", a, b);
}
int main(){
    int x = 10;
    int y = 20;
    func(x, y;  //i class error: missing closing parenthesis )
    arr[2 = x;  //k class error: missing closing bracket ]
    return 0;
}