const int MAX = 100;
int globalArray[100]; // 修改为编译期可求值的非负整数常量表达式
int add(int a, int b) {
    return a + b;
}
void printMessage() {
    printf("Hello, World!\n");
}
int main(){
    int localArray[10];
    const int VALUE = 10;
    int result = add(VALUE, 20);
    printf("Result: %d\n", result);
    int i = 0;
    for (i = 0; i < 10; i = i + 1) {
        localArray[i] = i * 2;
        printf("localArray[%d] = %d\n", i, localArray[i]);
    }
    printMessage();
    return 0;
}