const int MAX = 10;
int arr[MAX];
int sum(int a, int b) {
    return a + b;
}
int main(){
    int x = 5;
    int y = 10;
    int result;
    // Correct function call
    result = sum(x, y);
    printf("Sum: %d\n", result);
    // Function call with undefined function
    result = add(x, y;  // Missing closing parenthesis, should trigger j类错误
    return 0;
}