const int CONST_VAL = 5;
int global_var = 10;
int arr[5] = {1, 2, 3, 4, 5};
int add(int a, int b) {
    return a + b;
}
void print_sum(int a, int b) {
    int sum = add(a, b);
    printf("Sum: %d\n", sum);
    return;
}
int main(){
    int local_var = 20;
    int result = add(global_var, CONST_VAL); // Correct usage
    printf("Result: %d\n", result);
    print_sum(global_var, local_var); // Correct usage
    // Removed incorrect usage: passing three arguments to a function expecting two
    return 0;
}