const int SIZE = 10;
int array[SIZE];
int sum(int arr[]) {
    int total = 0;
    int i = 0;
    for (i = 0; i < SIZE; i = i + 1) {
        total = total + arr[i];
    }
    return total;
}
int main(){
    array[0 = 1; //Missing closing bracket error
    array[1 = 2; //Missing closing bracket error
    printf("Sum: %d\n", sum(array));
    return 0;
}