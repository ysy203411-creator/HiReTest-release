const int a = 5;
int b = 10;
int c[5] = {1, 2, 3, 4, 5};
int add(int x, int y) {
    return x + y;
}
int subtract(int x, int y) {
    return x - y;
}
int main(){
    int local_a = 3; //Rename local variable to avoid name collision
    int local_b = 20; //Rename local variable to avoid name collision
    int result = add(local_a, local_b);
    printf("Result: %d\n", result);
    int local_c[3]; //Rename local array to avoid name collision
    local_c[0] = subtract(local_b, local_a);
    printf("Subtraction: %d\n", local_c[0]);
    return 0;
}