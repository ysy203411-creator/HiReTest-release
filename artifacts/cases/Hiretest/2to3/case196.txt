const int N = 5;
int array[N] = {1, 2, 3, 4, 5};
int sum(int arr[], int len) {
    int total = 0;
    int i = 0;
    for (i = 0; i < len; i = i + 1) {
        total = total + arr[i];
    }
    return total;
}
int main(){
    int result = sum(array, N;
    printf("Sum: %d\n", result);
    return 0;
}