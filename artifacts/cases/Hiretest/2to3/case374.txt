const int MAX = 10;
int globalArray[MAX];
int sum(int a, int b) {
    return a + b;
}
int main(){
    int x = 5;
    int y = 10;
    int result;
    if (x | y) { //The incorrect single | symbol was used here; it should be || or another valid operator
        result = sum(x, y);
    } else {
        result = 0;
    }
    printf("Result: %d\n", result);
    return 0;
}