const int a = 10;
const int b = 120; // ASCII value of 'x'
int arr[5] = {1, 2, 3, 4, 5};
int add(int x, int y) {
    return x + y;
}
void printResult(int result) {
    printf("Result: %d\n", result);
}
int main(){
    int x = (a + arr[2]);
    int y = b; // Changed from char to int
    int z = add(x, 5);
    printResult(z);
    return 0;
}